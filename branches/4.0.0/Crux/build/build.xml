<?xml version="1.0" encoding="UTF-8"?>

<project default="dist" name="Crux" basedir="../">

	<tstamp>
		<format property="build.date" pattern="MM/dd/yyyy hh:mm:ss" />
	</tstamp>

	<property file="build/version.properties" />
	
	<property name="core-srcdir" value="src/core" />
	<property name="module-srcdir" value="src/module" />
	<property name="gadget-srcdir" value="src/ui/gadget" />
	<property name="gwt-srcdir" value="src/ui/gwt" />
	<property name="widgets-srcdir" value="src/ui/widgets" />
	<property name="themes-srcdir" value="src/ui/themes" />
	<property name="tools-srcdir" value="src/tools" />

	<property name="libdir" value="lib" />
	<property name="classesdir" value="bin/classes" />
	<property name="distdir" value="dist" />
	<property name="javadocdir" value="dist/javadoc" />
	<property name="crux.dev.name" value="crux-dev" />
	<property name="crux.runtime.name" value="crux-runtime" />
	<property name="crux.gadget.name" value="crux-runtime" />
	<property name="crux.libs.project" value="../CruxLibs/crux" />	

	<path id="build.classpath">
		<fileset dir="${libdir}">
			<include name="**/*.jar" />
		</fileset>
	</path>

	<typedef resource="org/tigris/subversion/svnant/svnantlib.xml" classpathref="build.classpath" />

	<!-- 
	========================================================================================================
		GenerateJavadoc
	========================================================================================================
	-->
	<target name="generate-javadoc">
		<javadoc encoding="UTF-8" destdir="${expandeddir}/docs" protected="true" source="1.5" classpathref="build.classpath">
			<fileset dir="${core-srcdir}" />
			<fileset dir="${module-srcdir}" />
			<fileset dir="${gadget-srcdir}" />
			<fileset dir="${gwt-srcdir}" />
			<fileset dir="${widgets-srcdir}" />
			<fileset dir="${themes-srcdir}" />
			<fileset dir="${tools-srcdir}" />
		</javadoc>
	</target>
	
	<!-- 
	========================================================================================================
		ExportJars
	========================================================================================================
	-->
	<target name="dist" description="Creates the crux zip file.">
		<delete dir="${distdir}" failonerror="no" includeemptydirs="yes" includes="**/*" />
		<mkdir dir="${distdir}" />
	</target>

	
	<!-- 
	========================================================================================================
		Export crux-dev.jar
	========================================================================================================
	-->
	<target name="export-crux-dev" description="Creates the crux-dev.jar file and exports it to the dist folder.">
		<delete  file="${distdir}/${crux.dev.name}.jar" failonerror="no" />
		
		<delete dir="${classesdir}" failonerror="no" />
		<mkdir dir="${classesdir}" />
		
		<javac destdir="${classesdir}" deprecation="true" target="1.5" source="1.5" debug="true" optimize="false" excludes="**/public/*" 
			   encoding="UTF-8">
			<src>
				<pathelement location="${core-srcdir}"/>
				<pathelement location="${module-srcdir}"/>
				<pathelement location="${gwt-srcdir}"/>
				<pathelement location="${tools-srcdir}"/>
			</src>
			<classpath refid="build.classpath" />
		</javac>

		<svn failonerror="false" javahl="false" svnkit="true">
			<wcversion path="${basedir}" />
		</svn>
		
		<jar jarfile="${distdir}/${crux.dev.name}.jar">
			<fileset dir="${classesdir}" excludes="**/public/*" />
			<fileset dir="${core-srcdir}" />
			<fileset dir="${module-srcdir}" />
			<fileset dir="${gwt-srcdir}" />
			<fileset dir="${tools-srcdir}" />
			<manifest>
				<attribute name="Build-Timestamp" value="${build.date}" />
				<attribute name="Implementation-Version" value="${version}" />
				<attribute name="Implementation-Revision" value="${revision.max}" />
			</manifest>
		</jar>
	</target>

	<!-- 
	========================================================================================================
		Export crux-runtime.jar
	========================================================================================================
	-->
	<target name="export-crux-runtime" description="Creates the crux-runtime.jar file and exports it to the dist folder.">
		<delete  file="${distdir}/${crux.runtime.name}.jar" failonerror="no" />
		
		<delete dir="${classesdir}" failonerror="no" />
		<mkdir dir="${classesdir}" />
		
		<javac destdir="${classesdir}" deprecation="true" target="1.5" source="1.5" debug="true" optimize="false" excludes="**/public/*" 
			   encoding="UTF-8">
			<src>
				<pathelement location="${core-srcdir}"/>
			</src>
			<classpath refid="build.classpath" />
		</javac>

		<svn failonerror="false" javahl="false" svnkit="true">
			<wcversion path="${basedir}" />
		</svn>
		
		<jar jarfile="${distdir}/${crux.runtime.name}.jar">
			<fileset dir="${classesdir}" >
				<exclude name="**/public/*" />
				<exclude name="br/com/sysmap/crux/core/client/**/*" />
				<exclude name="br/com/sysmap/crux/core/rebind/**/*" />
				<exclude name="br/com/sysmap/crux/core/declarativeui/**/*" />
				<exclude name="br/com/sysmap/crux/core/client" />
				<exclude name="br/com/sysmap/crux/core/rebind" />
				<exclude name="br/com/sysmap/crux/core/declarativeui" />
			</fileset>
			<fileset dir="${core-srcdir}" >
				<exclude name="br/com/sysmap/crux/core/client/**/*" />
				<exclude name="br/com/sysmap/crux/core/rebind/**/*" />
				<exclude name="br/com/sysmap/crux/core/declarativeui/**/*" />
				<exclude name="br/com/sysmap/crux/core/client" />
				<exclude name="br/com/sysmap/crux/core/rebind" />
				<exclude name="br/com/sysmap/crux/core/declarativeui" />
				<exclude name="br/com/sysmap/crux/core/Crux.gwt.xml" />
				<exclude name="META-INF/crux-ui.template.xslt" />
			</fileset>
			<manifest>
				<attribute name="Build-Timestamp" value="${build.date}" />
				<attribute name="Implementation-Version" value="${version}" />
				<attribute name="Implementation-Revision" value="${revision.max}" />
			</manifest>
		</jar>
	</target>
	
	<!-- 
	========================================================================================================
		Export crux-gadget.jar
	========================================================================================================
	-->
	<target name="export-crux-gadget" description="Creates the crux-dev.jar file and exports it to the dist folder.">
		<delete  file="${distdir}/${crux.gadget.name}.jar" failonerror="no" />
		
		<delete dir="${classesdir}" failonerror="no" />
		<mkdir dir="${classesdir}" />
		
		<javac destdir="${classesdir}" deprecation="true" target="1.5" source="1.5" debug="true" optimize="false" excludes="**/public/*" 
			   encoding="UTF-8">
			<src>
				<pathelement location="${gadget-srcdir}"/>
			</src>
			<classpath refid="build.classpath" />
		</javac>

		<svn failonerror="false" javahl="false" svnkit="true">
			<wcversion path="${basedir}" />
		</svn>
		
		<jar jarfile="${distdir}/${crux.gadget.name}.jar">
			<fileset dir="${classesdir}" excludes="**/public/*" />
			<fileset dir="${gadget-srcdir}" />
			<manifest>
				<attribute name="Build-Timestamp" value="${build.date}" />
				<attribute name="Implementation-Version" value="${version}" />
				<attribute name="Implementation-Revision" value="${revision.max}" />
			</manifest>
		</jar>
	</target>
	
</project>